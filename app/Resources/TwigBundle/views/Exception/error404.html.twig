{#
    This template is used to render errors of type HTTP 404 (Not Found)

    This is the simplest way to customize error pages in Symfony applications.
    In case you need it, you can also hook into the internal exception handling
    made by Symfony. This allows you to perform advanced tasks and even recover
    your application from some errors.
    See http://symfony.com/doc/current/cookbook/controller/error_pages.html
#}

{% extends 'templates/base_error.html.twig' %}
{% set _locale =  app.request.locale %}
{% block main %}
    <h1 class="ui header">{{ 'http_error.name'|trans({ '%status_code%': 404 }, "messages", _locale) }} -&nbsp;</h1><h2 class="ui header">{{ 'http_error_404.description'|trans({}, "messages", _locale)}}</h2>
    <hr>
    <p class="page-text">
        {{ 'http_error_404.suggestion'|trans({ '%url%': path('fmconference_homepage') }, "messages", _locale)|raw }}
    </p>
{% endblock %}
