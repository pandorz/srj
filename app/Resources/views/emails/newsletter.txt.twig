{% extends ':emails:email_tpl.txt.twig' %}

{% block main %}
    {{ title }}
    {% if subtitle %}
        {{ subtitle }}
    {% endif %}
    {{ 'newsletter.mail.texte'|trans({
        '%email%':data.email,
        })|raw }}
{% endblock %}