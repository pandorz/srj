{% extends ':emails:email_tpl.txt.twig' %}

{% block main %}
    {{ title }}
    {% if subtitle %}
        {{ subtitle }}
    {% endif %}
    {{ 'contact.mail.texte'|trans({
        '%nom%':data.nom,
        '%prenom%':data.prenom,
        '%objet%':data.objet,
        '%message%':data.message,
        '%email%':data.email,
        })|striptags }}
{% endblock %}