{% extends ':emails:email_tpl.html.twig' %}

{% block main %}
    <h1>{{ title }}</h1>
    {% if subtitle %}
        <p class="lead">{{ subtitle }}</p>
    {% endif %}
    <p>
        {{ 'lost_pwd.mail.message'|trans({'%username%': data.username, '%confirmationUrl%': data.confirmationUrl}) }}
    </p>
{% endblock %}