{% extends ':emails:email_tpl.html.twig' %}

{% block main %}
    <h1>{{ title }}</h1>
    {% if subtitle %}
        <p class="lead">{{ subtitle }}</p>
    {% endif %}
    <p>
        {{ 'contact.mail.texte'|trans({})|raw }}
    </p>
{% endblock %}