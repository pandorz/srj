{% extends ':emails:email_tpl.html.twig' %}

{% block main %}
    {{ title }}
    {% if subtitle %}
        {{ subtitle }}
    {% endif %}
    {{ 'lost_pwd.mail.message'|trans({'%username%': data.user.username, '%confirmationUrl%': data.confirmationUrl}) }}
{% endblock %}