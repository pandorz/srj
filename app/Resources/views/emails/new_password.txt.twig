{% extends ':emails:email_tpl.html.twig' %}

{% block main %}
    {{ title }}
    {% if subtitle %}
        {{ subtitle }}
    {% endif %}
    {{ 'lost_pwd.mail.message'|trans({'%username%': data.username, '%confirmationUrl%': data.confirmationUrl})|striptags }}
{% endblock %}