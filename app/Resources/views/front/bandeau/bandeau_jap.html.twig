{% trans_default_domain 'front' %}
<div class="hero">
    <h1 class="heroTitle">{{ 'bandeau.title'|trans }}</h1>
    <div class="heroJPbaseline" lang="ja">{{ 'bandeau.base_line'|trans }}</div>
    <div class="userGuide">
        <a class="btn btn-blank" href="{{path('presentation_japonaise')}}" lang="ja">{{ 'general.title.nihon'|trans }}</a>
        {% if lien_adhesion() is not empty %}
        <a class="btn" href="{{ lien_adhesion() }}" target="_blank">{{ 'general.button.be_member'|trans }}</a>
        {% endif %}
    </div>
</div>